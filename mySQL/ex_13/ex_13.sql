SELECT room_number, seats FROM coding.rooms where seats >=0 order by seats asc;

/*in this case the db gui aka phpmyadmin is necessary because file contains a table called Rooms, from where we need to query*/
